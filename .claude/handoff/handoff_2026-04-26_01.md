# 🤝 HANDOFF — 2026-04-26 01:00

## このセッションのゴール（達成済 ✅）

CKO v4.0 / kiji-rag の **画像盲目問題** を解決。記事LP内の画像を Gemini Vision で説明文化し、embeddingで検索可能にした。

## 完了済み ✅

### 1. 画像取得3経路ルール確定 → memory刻み
- 自社案件: `getArticleHtml` MCP で `data-src` 経由で実URL自動解決
- 他社案件: WebFetchではlazy.png placeholderしか取れない（要Chrome MCP/手動スクショ）
- memory: `feedback_kiji_image_acquisition.md`

### 2. 画像→Description→Embeddingパイプライン実装
- 新規ツール: `.claude/knowledge/kiji-rag/tools/describe_images.py`
  - Gemini 2.5 Flash で各画像を1〜3文に説明文化
  - lazy.png / pixel.gif は自動スキップ
  - `image_descriptions.json` キャッシュ機能あり
- 改修: `.claude/knowledge/kiji-rag/tools/embed_chunks.py`
  - `image_description` フィールドが入っていたら自動で `[画像説明]` プレフィックスでembed_textに含める
- スキル: `kiji-rag-add` ver.1.2 — Step 8.5 として組み込み済

### 3. fuyumotto 全画像description化 + 再embedding
- 27 image chunks を全部 describe（成功率 100%）
- 108 chunks 再embedding完了（3072dim）
- 検索動作確認: 画像系クエリで上位ヒットするように

### 4. 関連commit
- 過去commitで wakiga / fuyumotto / sirojam を順次RAG化
- 今回のコミット予定: describe_images.py + embed_chunks.py改修 + fuyumotto再embedding結果

## 残タスク 🚧

### A. シロジャム記事LP（クロコス社）の画像取得
- 現状: `20260426_sirojam_hand` は WebFetch経由 → 画像50枚すべて lazy.png
- 必要アクション:
  1. **Claude in Chrome MCP** でブラウザ起動 → `https://sb-hand.myown-page.com/ab/sirojam_yda001` を開く
  2. ページ全体を最下部までスクロール（lazy-load発火）
  3. JavaScriptで `document.querySelectorAll('img').forEach(i=>console.log(i.dataset.src||i.src))` 実行
  4. または DOM全文取得 → `data-src` を抜き取り
  5. blocks_raw.json の画像URLを差し替え → `describe_images.py --force` 再実行 → `embed_chunks.py` 再実行

### B. 全RAG既存記事の画像description適用
- 対象: `20260424_aurelie_megumi_kakusen` / `20260424_proust_cream2_wakiga`
- 手順:
  ```bash
  python3 .claude/knowledge/kiji-rag/tools/describe_images.py \
    .claude/knowledge/kiji-rag/articles/<article_id>/chunks.json
  python3 .claude/knowledge/kiji-rag/tools/embed_chunks.py \
    .claude/knowledge/kiji-rag/articles/<article_id>/chunks.json
  ```
- ※ wakiga は元から画像0本なので不要、aureleiのみ要処理

### C. CKO v4.0 残り7体のagent仕様調整
（前セッションから継続）
- kiji-prescriber-crew: swing_mode B-modified 実装
- kiji-rewriter: 「削る」処方明文化
- kiji-flow (agent化): chunk総量・funnel別配分評価軸追加
- kiji-diff: chunk数比/funnel密度比較指標
- kiji-visual-rhythm: 動画連続検出指標 + **画像description類似度比較も追加検討**

## 注意点 ⚠️

### 画像取得3経路の使い分け（絶対忘れない）
| 案件タイプ | 使用ツール |
|---|---|
| 自社（ヨミテ等） | `getArticleHtml` MCP |
| 他社（クロコス等） | Claude in Chrome MCP / Playwright |
| MCP越権・アカウント外 | 手動スクショ → Vision LLM |

### 古いRAG記事は画像盲目状態
- 既存4記事のうち wakiga(画像0)以外は **画像description未注入**
- 画像系クエリの精度が出ない場合、まずこれを疑う

### embed_text 構成（ver.1.2）
```
[文脈] context_hint
[ファネル位置] funnel_position
content_text
[キャプション] captions
[alt] alts
[画像説明] image_description ← NEW
[主要訴求要素] dominant_elements
[訴求層] dominant_layer
```

## 次セッション用ワンライナー
> handoff_2026-04-26_01.md を読んで、(A) シロジャム記事の画像をChrome MCPで取り直す → (B) aurelei記事も画像description化 → (C) CKO v4.0 残り7体着手、の順で進めて。
