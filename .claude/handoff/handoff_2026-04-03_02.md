---
# 🤝 HANDOFF — 2026-04-03 #2

## このセッションのゴール
編集AI v13の実装（句読点フィルタ・無音トリム・参考動画レベル）

## 完了済み ✅
- **句読点フィルタ** (`clean_telop_text()`) — `、。「」`を自動除去、`…！？※`は残す
- **無音トリム** (`trim_silence()`) — ffmpeg silenceremoveで先頭・末尾の無音カット（-40dB）、TTS直後に適用
- **参考動画レベル** — CSV `■ 参考動画設定` の `反映レベル: 1/2/3` をパース、`analyze_reference_video()` で分岐
- 構文チェック通過済み（py_compile OK）

## 次セッションでやること 🚀
1. **実案件でv13テスト実行** — 句読点フィルタ・無音トリムの効果を動画で確認
2. **Lv.3 完全トレースの詳細実装** — 現在は`_trace_mode`フラグのみ。各カット秒数を台本シーンに反映する処理が未実装
3. **カントク君スキル更新** — テロップ判断軸（出す/出さないルール）をknowledgeに追加
4. **アプリ化**（保留中）— Google Sheets API連携は将来対応。設計は済み

## 注意点 ⚠️
- `analyze_clip_with_gemini()` のプロンプトコア部分は変えない（v11の教訓）
- `pydub` は未インストール。無音トリムはffmpegで実装済みなので不要
- 句読点フィルタは `make_telop()` の冒頭で適用。`calc_dynamic_font_size()` もクリーニング後テキストで計算するようにした

## 次セッション用ワンライナー
> 編集AI v13の実案件テストをしよう。edit_ai_v2.py に句読点フィルタ・無音トリム・参考動画レベルを実装済み。実際に動画を生成して効果を確認したい。
---
