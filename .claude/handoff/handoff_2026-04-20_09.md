---
# 🤝 HANDOFF — 2026-04-20 09:40

## このセッションのゴール
**FV量産Studio KOSURIちゃんのv2〜v4反復改善**。Video Use(browser-useチーム)の哲学を吸収しつつ、ヨミテ4商品（gungun / proust / onmyskin / RKL）のFV自動生成を「実験設計としてクリーンに（変数=画像の中身のみ）」仕上げる。

---

## 完了済み ✅

### v3で実装（前セッションから引き継ぎ）
- ✅ 商品プロファイル（`.claude/clients/yomite/{product}/kosuri-profile.yaml`）4本 + `profile_loader.py`
- ✅ `_analyze_video_for_fv`に`caption_segments`タイムライン解析追加
- ✅ テロップを透明背景PNG+overlay時間指定で「画像と独立して画面固定」
- ✅ Meta/TikTokセーフエリア準拠Y配置 (hook_top=20%, kick_bottom=72%)
- ✅ 注釈自動追加廃止 / xfade廃止（単純concat）
- ✅ 極太フォント(W9/Black) + 自動フィット + emphasis黄色

### v4で実装（このセッション）
- ✅ **9:16強制正規化**: `_normalize_to_9_16`新設 → 生成画像がどんな比率でも必ず1080x1920にcrop+resize。バグ再発ゼロ
- ✅ **モーション固定**: 全10パターン同じmotion_type使用（`primary_motion` or `slow_zoom_to_face`フォールバック）
- ✅ **テロップ見せ方完全統一**: 全セグメント`hook_top / xl / double_stroke`固定 + 短文自動拡大（画面幅75-88%に自動調整）
- ✅ **LLMスキーマ簡素化**: caption/motion系パラメータを要求せず画像方針のみ返させる
- ✅ 4プロファイルyamlに`primary_motion`フィールド追加
- ✅ サーバー起動問題の根本修正（bash→zsh sourceで環境変数読み込み）

### デバッグ完了
- ✅ **20分ハング原因特定**: `bash`起動だと`~/.zshrc`がsourceされず`GEMINI_API_KEY_1`が空文字 → 空キーでGeminiが変なハング。`zsh -c 'source ~/.zshrc; ...'`で解決
- ✅ Gemini API両方とも実測応答OK: gemini-2.0-flash 0.9s / gemini-2.5-flash-image 9.6s

---

## 次セッションでやること 🚀

### 1. v4テスト結果の確認（優先）
ユーザーは「いいね！」と言ったがハンドオフに移ったので、実際の10パターン生成結果を一緒に見る必要あり。
- デスクトップの `FV生成テスト v4/` もしくは最新テストフォルダを確認
- 10本すべて「同じテロップ・同じ動き・同じ位置」で画像だけ10種類違うこと
- 9:16バグゼロ確認
- もしズレがあれば即修正

### 2. 「2つ目のミッション」を聞く ← ユーザーからの明示指示
> これ適応したら2つ目のミッションを言うので、まずは上記をお願いします

v4適用完了したのでこのミッションが次の目玉。

### 3. 残タスク（優先度順）
- **キャンセル機能のバックエンド完全化** — UIにはボタン存在するが `/cancel/<job_id>` エンドポイントと run_*_job のcancel check実装が半端な可能性。確認と仕上げ
- **画像の質（インパクト）向上** — v3フィードバック#3: 「AI生成特性を活かしきれてない、人間が思いつく範疇」に応答
- **DProの伸びてる動画のFVビジュアルフック研究** — v3フィードバック#4: ジャンル問わずTOP広告を調査してビジュアルフック発掘
- **Dockerfile更新** — W9相当のBlack Japanese font確保 (`fonts-noto-cjk-extra`等)
- **Vision検品ガード** — 生成画像にテキスト/ロゴ/UI要素あれば最大3回再生成

---

## プラン詳細（設計メモ）

### サーバー稼働状態（セッション終了時点）
- **PID 89885 (parent) / 89906 (worker)** が localhost:5050 でリスン中
- 環境変数 `GEMINI_API_KEY_1/2/3` 認識済み
- 4商品プロファイル全部ロード済み
- ユーザーテスト中の可能性あり → **killする前に確認**

### 重要ファイル
- `video-ai/fv_studio/app.py` — メイン（~2900行、v4）
- `video-ai/fv_studio/profile_loader.py` — プロファイルローダー
- `.claude/clients/yomite/{gungun,proust,onmyskin,rkl}/kosuri-profile.yaml` — 4商品
- `.claude/knowledge/motion-db.md` — DPro学習モーションDB（v1.0）
- `video-ai/fv_studio/templates/index.html` — UI（商品セレクタ + キャンセルボタン）
- メモリ: `feedback_kosuri_caption_rule.md` — 「変数は1つ」本質ルール

### v4の重要な実装
- `_normalize_to_9_16(image_path)` — PillowでForced 9:16 crop + resize。`_gemini_generate_image`成功時に必ず呼ぶ
- `_build_caption_timeline_from_pattern` — v4で全固定化（hook_top/xl/double_stroke）。pattern引数は事実上無視
- `FIXED_MOTION` in `run_fv_generate_job` — ループ外で1回決定、全10本で同一
- `_draw_caption_layers_on_canvas` — 短文拡大ロジック: Step1縮小→Step2拡大（target_min_px=75%, max_width_px=88%）

### サーバー起動コマンド（必須）
```bash
# 必ず zsh + source ~/.zshrc で起動しないとGEMINI_API_KEYが空になる
zsh -c 'source ~/.zshrc 2>/dev/null; cd /Users/ca01224/Desktop/一進VOYAGE号/video-ai/fv_studio && python3 app.py'
```

### ユーザー哲学の再確認（絶対にブレない）
- **音声・テロップ文字・出現タイミング = 全部固定**
- **モーション = 10本で同じ**
- **テロップ見せ方 = 10本で同じ**
- **注釈 = AI側で自動追加しない**（ユーザーが後工程で手動）
- **変わるのは「画像の中身」だけ** — これが唯一の検証変数

---

## 注意点 ⚠️

1. **bashからpython3 app.pyを直接起動するとハングする** — 必ず`zsh -c 'source ~/.zshrc && python3 app.py'`
2. **ユーザー指示で「交互振り分け」「自動注釈追加」「パターン別テロップ見せ方」は全て廃止済み** → 復活させないこと
3. **Gemini 2.5 Flash Image は aspectRatio を直接指定できない** → プロンプト誘導 + Pillow強制crop の二段構え
4. **V11-V14（Split-Screen / 医療画像 / 専門家 / 商品単品）は実装済み**（app.py:1699-1741）
5. **server プロセス PID 89885/89906 が生きてる可能性** → テスト継続中なら残す、不要なら `lsof -ti:5050 | xargs kill`
6. **GCS register-gcs endpoint が500返す**（GCS未設定）→ローカルuploadで問題なく動くので無視でOK

---

## 次セッション用ワンライナー

```
.claude/handoff/handoff_2026-04-20_09.md を読んで、KOSURI v4テスト結果の確認と「2つ目のミッション」から続きを始めて。最初はユーザーのv4テスト結果（デスクトップFV生成テストv4 or v5フォルダ）を見て、問題なければ2つ目のミッションを聞く。
```

---

## 実装前のユーザーフィードバック記録（次セッションが読むべき）

### v3までの明確な改善ポイント（全てv4で対応済み）
1. 縦横比バグ → `_normalize_to_9_16`で遮断
2. 画像見せ方のシンプルさ → v4では画像の中身が唯一の変数なので、次フェーズでImpact向上が課題
3. テロップ出現タイミングずれ → caption_segments時系列解析 + 全セグメント固定placement
4. 「変数は1つ＝画像中身」 → v4で完全実装

### 未対応のv3フィードバック
- **画像の質（インパクト）**: 「AI全生成の特性を活かしきれてない、人間が思いつく範疇」→ 2つ目ミッションと関係する可能性大
- **DPro勝ち広告FVビジュアル研究**: ジャンル不問でTOPを見て学習
---
